<h4>Categorias: </h4>

{{#each categorias}}
    <div class="card">
        <div class="card-body">
            <a href="/categorias/{{slug}}"><h5 class="card-title">{{nome}}</h5></a>
        </div>
    </div>
{{else}}
    <h5>Não há categorias cadastradas</h5>
{{/each}}